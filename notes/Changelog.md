### Version `2.0.2`

- Fiat fees now round to 2 decimal points
- `Countup` element added to fiat fee price
- App version now shows in UI

### Version `2.0.3` (next)

- Automate `manifest.json` version updates (when npm run package command is used -> update the version in manifest.json, run the build, run the zip script)

### Version `2.0.4`

- Fixed floating precision bug in `PriceCard` component (was not applying decimal precision logic to deal with values > 10)
- Added app scale settings functionality (full end-to-end feature)
- Add app description to `manifest.json` to have it show on extensions page

Access real-time Ethereum fee estimates for various transaction types,

Stay in sync with Ethereum always with instant access to real-time price discovery, network conditions, spot prices, and transaction fee estimates for various transactions dynamically computed and served to you as new blocks get
